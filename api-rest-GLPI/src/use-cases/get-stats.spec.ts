import { describe, test } from "vitest";

describe("Get Stats Use Case", () => {
  test.skip("Should be able get list stats", async () => {
  });

  test.skip("Should be able to return stats list empty", async () => {
  });
});
