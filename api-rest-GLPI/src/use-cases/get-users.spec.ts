import { describe, test } from "vitest";

describe("Get Users Use Case", () => {
  test.skip("Should be able get list users", async () => {
  });

  test.skip("Should be able to return users list empty", async () => {
  });
});
