import { describe, test } from "vitest";

describe("Get Tickets Use Case", () => {
  test.skip("Should be able get list tickets", async () => {
  });

  test.skip("Should be able to return tickets list empty", async () => {
  });
});
